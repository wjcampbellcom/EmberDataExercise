import { functionCaller } from 'pricing-driver/helpers/function-caller';
import { module, test } from 'qunit';

module('Unit | Helper | function caller');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = functionCaller([42]);
  assert.ok(result);
});
